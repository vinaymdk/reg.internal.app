specVersion: "v1beta"
serviceId: "quasartest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quasartest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
